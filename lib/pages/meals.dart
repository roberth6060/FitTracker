import 'package:flutter/material.dart';

class MealsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Meals Page", style: TextStyle(fontSize: 24)));
  }
}
