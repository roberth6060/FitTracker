import 'package:flutter/material.dart';

class MoodPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Mood Tracker Page", style: TextStyle(fontSize: 24)),
    );
  }
}
